<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <!-- Header row -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="6dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Progress"
            android:textSize="13sp"
            android:textColor="#111827"
            android:textStyle="bold"
            android:layout_centerVertical="true" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/progress_widget_updated_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="10sp"
                android:textColor="#9CA3AF"
                android:layout_marginEnd="8dp" />

            <TextView
                android:id="@+id/progress_widget_refresh_btn"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:text="↻"
                android:textSize="16sp"
                android:textColor="#E8632A"
                android:gravity="center" />
        </LinearLayout>
    </RelativeLayout>

    <!-- Divider -->
    <TextView android:text=""
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#F3F4F6"
        android:layout_marginBottom="4dp" />

    <!-- Scrollable tracker list -->
    <ListView
        android:id="@+id/progress_widget_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="0dp" />

    <!-- Empty state -->
    <TextView
        android:id="@+id/progress_widget_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="No trackers yet — open the app to add one"
        android:textSize="12sp"
        android:textColor="#6B7280"
        android:gravity="center"
        android:visibility="gone" />

    <!-- Divider -->
    <TextView android:text=""
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#F3F4F6"
        android:layout_marginTop="4dp" />

    <!-- Open app footer -->
    <TextView
        android:id="@+id/progress_widget_open_app"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Open App →"
        android:textSize="11sp"
        android:textColor="#E8632A"
        android:gravity="end"
        android:paddingTop="6dp" />

</LinearLayout>
